generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL") // URL do pool (pgbouncer) do Supabase
  directUrl = env("DIRECT_URL") // URL direta do banco (sem pool), usada para migrações
}
